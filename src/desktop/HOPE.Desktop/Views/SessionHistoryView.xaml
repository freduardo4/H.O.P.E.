<UserControl x:Class="HOPE.Desktop.Views.SessionHistoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="800"
             Background="#121212">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="SESSION HISTORY" Foreground="White" FontSize="32" FontWeight="Bold"/>
            <TextBlock Text="View past diagnostic sessions" Foreground="#AAAAAA" FontSize="14"/>
        </StackPanel>

        <!-- Refresh Button -->
        <Button Grid.Row="1" Content="Refresh" Command="{Binding RefreshSessionsCommand}"
                HorizontalAlignment="Left" Padding="20,10" FontSize="14" FontWeight="SemiBold"
                Background="#2979FF" Foreground="White" BorderThickness="0" Margin="0,0,0,15">
            <Button.Resources>
                <Style TargetType="Border">
                    <Setter Property="CornerRadius" Value="6"/>
                </Style>
            </Button.Resources>
        </Button>

        <!-- Sessions List -->
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Session List -->
            <Border Grid.Column="0" Background="#1E1E1E" CornerRadius="10" Padding="10" Margin="0,0,8,0">
                <StackPanel>
                    <TextBlock Text="Sessions" Foreground="White" FontSize="16" FontWeight="SemiBold" Margin="10,5,0,10"/>
                    <ListView ItemsSource="{Binding Sessions}" 
                              SelectedItem="{Binding SelectedSession}"
                              Background="Transparent" BorderThickness="0" MaxHeight="400">
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <Border Background="#252525" CornerRadius="6" Margin="0,3" Padding="12,10">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <StackPanel Grid.Column="0">
                                            <TextBlock Text="{Binding FormattedDate}" Foreground="White" FontSize="14" FontWeight="SemiBold"/>
                                            <TextBlock Text="{Binding FormattedDuration}" Foreground="#888888" FontSize="12"/>
                                        </StackPanel>
                                        <TextBlock Grid.Column="1" Text="›" Foreground="#666666" FontSize="20" VerticalAlignment="Center"/>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                        <ListView.ItemContainerStyle>
                            <Style TargetType="ListViewItem">
                                <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                                <Setter Property="Padding" Value="0"/>
                                <Setter Property="Margin" Value="0"/>
                                <Setter Property="Background" Value="Transparent"/>
                            </Style>
                        </ListView.ItemContainerStyle>
                    </ListView>
                    <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                        <Button Content="View Details" Command="{Binding ViewSessionDetailsCommand}"
                                IsEnabled="{Binding SelectedSession, Converter={StaticResource NullToBooleanConverter}}"
                                Padding="15,8" Margin="0,0,8,0"
                                Background="#333333" Foreground="White" BorderThickness="0">
                            <Button.Resources>
                                <Style TargetType="Border">
                                    <Setter Property="CornerRadius" Value="4"/>
                                </Style>
                            </Button.Resources>
                        </Button>
                        <Button Content="Export CSV" Command="{Binding ExportToCsvCommand}"
                                IsEnabled="{Binding SelectedSession, Converter={StaticResource NullToBooleanConverter}}"
                                Padding="15,8" Margin="0,0,8,0"
                                Background="#1B5E20" Foreground="White" BorderThickness="0">
                            <Button.Resources>
                                <Style TargetType="Border">
                                    <Setter Property="CornerRadius" Value="4"/>
                                </Style>
                            </Button.Resources>
                        </Button>
                        <Button Content="Export PDF" Command="{Binding ExportToPdfCommand}"
                                IsEnabled="{Binding SelectedSession, Converter={StaticResource NullToBooleanConverter}}"
                                Padding="15,8"
                                Background="#C62828" Foreground="White" BorderThickness="0">
                            <Button.Resources>
                                <Style TargetType="Border">
                                    <Setter Property="CornerRadius" Value="4"/>
                                </Style>
                            </Button.Resources>
                        </Button>
                    </StackPanel>
                </StackPanel>
            </Border>

            <!-- Session Data Preview -->
            <Border Grid.Column="1" Background="#1E1E1E" CornerRadius="10" Padding="10" Margin="8,0,0,0">
                <StackPanel>
                    <TextBlock Text="Session Data" Foreground="White" FontSize="16" FontWeight="SemiBold" Margin="10,5,0,10"/>
                    <ListView ItemsSource="{Binding SessionReadings}" 
                              Background="Transparent" BorderThickness="0" MaxHeight="400">
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <Border Background="#252525" CornerRadius="4" Margin="0,2" Padding="10,6">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="80"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Grid.Column="0" Text="{Binding Timestamp, StringFormat=HH:mm:ss}" 
                                                   Foreground="#888888" FontSize="11"/>
                                        <TextBlock Grid.Column="1" Text="{Binding Name}" 
                                                   Foreground="#CCCCCC" FontSize="12"/>
                                        <TextBlock Grid.Column="2" Foreground="#00E676" FontSize="12" FontWeight="SemiBold">
                                            <Run Text="{Binding Value, StringFormat={}{0:F1}}"/>
                                            <Run Text="{Binding Unit}" Foreground="#888888"/>
                                        </TextBlock>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                        <ListView.ItemContainerStyle>
                            <Style TargetType="ListViewItem">
                                <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                                <Setter Property="Padding" Value="0"/>
                                <Setter Property="Margin" Value="0"/>
                                <Setter Property="Background" Value="Transparent"/>
                            </Style>
                        </ListView.ItemContainerStyle>
                    </ListView>
                </StackPanel>
            </Border>
        </Grid>

        <!-- Status Bar -->
        <Border Grid.Row="3" Background="#1E1E1E" CornerRadius="6" Padding="12,8" Margin="0,10,0,0">
            <TextBlock Text="{Binding StatusMessage}" Foreground="#AAAAAA" FontSize="13"/>
        </Border>
    </Grid>
</UserControl>
