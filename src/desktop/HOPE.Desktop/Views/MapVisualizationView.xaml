<UserControl x:Class="HOPE.Desktop.Views.MapVisualizationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1000"
             Background="#121212">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0" Text="ECU MAP VISUALIZATION" Foreground="White" FontSize="32" FontWeight="Bold" FontFamily="Segoe UI Semibold" Margin="0,0,0,10"/>
        
        <!-- Controls -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,20">
            <Button Content="READ FROM ECU" Command="{Binding ReadEcuMapCommand}" 
                    Padding="20,10" Background="#2979FF" Foreground="White" BorderThickness="0" FontWeight="Bold">
                <Button.Style>
                    <Style TargetType="Button">
                        <Style.Triggers>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="#333333"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
            <TextBlock Text="{Binding StatusMessage}" Foreground="#AAAAAA" VerticalAlignment="Center" Margin="20,0,0,0" FontSize="14"/>
        </StackPanel>

        <!-- Map Grid -->
        <Border Grid.Row="2" Background="#1E1E1E" CornerRadius="8" Padding="10">
            <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
                <ItemsControl ItemsSource="{Binding MapData}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <!-- Row Axis Label (RPM Mock) -->
                                <Border Width="60" Height="40" Background="#252525" BorderBrush="#121212" BorderThickness="1">
                                    <TextBlock Text="{Binding Index, ConverterParameter=500, Converter={StaticResource RpmAxisConverter}}" Foreground="#888888" VerticalAlignment="Center" HorizontalAlignment="Center" FontSize="11"/>
                                </Border>
                                
                                <ItemsControl ItemsSource="{Binding Values}">
                                    <ItemsControl.ItemsPanel>
                                        <ItemsPanelTemplate>
                                            <StackPanel Orientation="Horizontal"/>
                                        </ItemsPanelTemplate>
                                    </ItemsControl.ItemsPanel>
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Border Width="60" Height="40" BorderBrush="#121212" BorderThickness="1">
                                                <Border.Style>
                                                    <Style TargetType="Border">
                                                        <Setter Property="Background" Value="{Binding ., Converter={StaticResource ValueToHeatmapConverter}}"/>
                                                    </Style>
                                                </Border.Style>
                                                <TextBlock Text="{Binding ., StringFormat={}{0:F1}}" Foreground="White" VerticalAlignment="Center" HorizontalAlignment="Center" FontSize="12" FontWeight="SemiBold"/>
                                            </Border>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </StackPanel>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Border>

        <TextBlock Grid.Row="3" Text="* Values represent Target AFR in Mode 01" Foreground="#555555" Margin="0,10,0,0" FontSize="11" FontStyle="Italic"/>
    </Grid>
</UserControl>
