<UserControl x:Class="HOPE.Desktop.Views.MultiViewEditor"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:prism="http://prismlibrary.com/"
             xmlns:lvc="clr-namespace:LiveChartsCore.SkiaSharpView.WPF;assembly=LiveChartsCore.SkiaSharpView.WPF"
             prism:ViewModelLocator.AutoWireViewModel="True"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1200"
             Background="#121212">

    <UserControl.Resources>
        <Style TargetType="TabItem">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabItem">
                        <Border Name="Border" BorderThickness="0,0,0,2" BorderBrush="Transparent" Padding="15,10" Margin="0,0,5,0">
                            <ContentPresenter x:Name="ContentSite"
                                            VerticalAlignment="Center"
                                            HorizontalAlignment="Center"
                                            ContentSource="Header"
                                            RecognizesAccessKey="True"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Border" Property="BorderBrush" Value="#2979FF"/>
                                <Setter Property="Foreground" Value="#2979FF"/>
                                <Setter Property="FontWeight" Value="Bold"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="False">
                                <Setter Property="Foreground" Value="#888888"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header Controls -->
        <Grid Grid.Row="0" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Orientation="Vertical">
                <TextBlock Text="{Binding MapName, FallbackValue='Fuel Map (High Octane)'}" Foreground="White" FontSize="24" FontWeight="Bold"/>
                <TextBlock Text="{Binding MapAddress, StringFormat='Address: 0x{0:X}'}" Foreground="#888" FontSize="14" Margin="0,5,0,0"/>
            </StackPanel>

            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <Button Content="Rescale Axes" Command="{Binding OpenAxisEditorCommand}" Background="#333" Foreground="White" Padding="15,8" Margin="0,0,10,0" BorderThickness="0"/>
                <Button Content="Save Changes" Command="{Binding SaveCommand}" Background="#2979FF" Foreground="White" Padding="15,8" BorderThickness="0"/>
            </StackPanel>
        </Grid>

        <!-- Main Content (Tabs) -->
        <TabControl Grid.Row="1" Background="Transparent" BorderThickness="0">
            
            <!-- 3D Surface View -->
            <TabItem Header="3D SURFACE">
                <Grid Background="#1E1E1E">
                    <Viewport3D>
                        <Viewport3D.Camera>
                            <PerspectiveCamera Position="20, -20, 30" LookDirection="-1, 1, -1" UpDirection="0, 0, 1" FieldOfView="45"/>
                        </Viewport3D.Camera>
                        <ModelVisual3D>
                            <ModelVisual3D.Content>
                                <Model3DGroup>
                                    <DirectionalLight Color="White" Direction="-1,-1,-3" />
                                    <AmbientLight Color="#404040"/>
                                    <GeometryModel3D>
                                        <GeometryModel3D.Geometry>
                                            <MeshGeometry3D Positions="{Binding SurfacePoints}"
                                                            TriangleIndices="{Binding SurfaceIndices}"/>
                                        </GeometryModel3D.Geometry>
                                        <GeometryModel3D.Material>
                                            <DiffuseMaterial>
                                                <DiffuseMaterial.Brush>
                                                    <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                                        <GradientStop Color="#00BFFF" Offset="0.0"/>
                                                        <GradientStop Color="#2979FF" Offset="1.0"/>
                                                    </LinearGradientBrush>
                                                </DiffuseMaterial.Brush>
                                            </DiffuseMaterial>
                                        </GeometryModel3D.Material>
                                        <GeometryModel3D.BackMaterial>
                                             <DiffuseMaterial Brush="#333"/>
                                        </GeometryModel3D.BackMaterial>
                                    </GeometryModel3D>
                                </Model3DGroup>
                            </ModelVisual3D.Content>
                        </ModelVisual3D>
                    </Viewport3D>
                    
                    <!-- Overlay Controls for 3D -->
                    <Border VerticalAlignment="Bottom" HorizontalAlignment="Right" Margin="20" Background="#99000000" Padding="10" CornerRadius="4">
                        <StackPanel>
                            <TextBlock Text="Rotate: Right-Click + Drag" Foreground="#AAA" FontSize="10"/>
                            <TextBlock Text="Zoom: Scroll" Foreground="#AAA" FontSize="10"/>
                        </StackPanel>
                    </Border>
                </Grid>
            </TabItem>

            <!-- Tabular View -->
            <TabItem Header="TABULAR">
                <DataGrid ItemsSource="{Binding TabularData}" 
                          AutoGenerateColumns="True" 
                          CanUserAddRows="False"
                          Background="#1E1E1E"
                          RowBackground="#1E1E1E"
                          AlternatingRowBackground="#252525"
                          Foreground="White"
                          BorderThickness="0"
                          GridLinesVisibility="All"
                          HorizontalGridLinesBrush="#333"
                          VerticalGridLinesBrush="#333">
                    <!-- Note: In a real implementation, we might use a custom DataGrid to handle dynamic columns for X/Y axes labels properly -->
                </DataGrid>
            </TabItem>

            <!-- 2D Chart View -->
            <TabItem Header="2D CHART">
                <Grid Background="#1E1E1E">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    
                    <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="10,10,0,10">
                        <TextBlock Text="Row:" Foreground="White" VerticalAlignment="Center" Margin="0,0,10,0"/>
                        <ComboBox ItemsSource="{Binding AxisRows}" SelectedItem="{Binding SelectedRow}" Width="100"/>
                    </StackPanel>

                    <lvc:CartesianChart Grid.Row="1" Series="{Binding ChartSeries}" XAxes="{Binding XAxes}" YAxes="{Binding YAxes}" Margin="10">
                    </lvc:CartesianChart>
                </Grid>
            </TabItem>

            <!-- Hex View -->
            <TabItem Header="HEX">
                <Grid Background="#1E1E1E">
                    <TextBox Text="{Binding HexContent}" 
                             FontFamily="Consolas" 
                             FontSize="14" 
                             Foreground="#00FF00" 
                             Background="#111" 
                             BorderThickness="0" 
                             AcceptsReturn="True" 
                             VerticalScrollBarVisibility="Auto"
                             Padding="10"/>
                </Grid>
            </TabItem>

        </TabControl>
    </Grid>
</UserControl>
