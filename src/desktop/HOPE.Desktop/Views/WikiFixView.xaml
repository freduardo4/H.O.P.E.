<UserControl x:Class="HOPE.Desktop.Views.WikiFixView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:HOPE.Desktop.Views"
             xmlns:vm="clr-namespace:HOPE.Desktop.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800"
             Background="#0A0A0A">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
    </UserControl.Resources>
    
    <Grid Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="280"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Grid.ColumnSpan="2" Margin="0,0,0,20">
            <TextBlock Text="WIKI-FIX COMMUNITY" FontSize="28" FontWeight="Bold" Foreground="White"/>
            <Rectangle Height="2" Fill="#00AAFF" HorizontalAlignment="Left" Width="100" Margin="0,5,0,0"/>
            <TextBlock Text="Verified repair patterns and community solutions." 
                       Foreground="#AAAAAA" Margin="0,5,0,0"/>
        </StackPanel>

        <Grid Grid.Row="1" Grid.ColumnSpan="2" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="150"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBox Text="{Binding SearchQuery, UpdateSourceTrigger=PropertyChanged}" 
                      Background="#1A1A1A" Foreground="White" BorderBrush="#333333" 
                      Padding="12,8" FontSize="14" Margin="0,0,10,0"/>
            
            <TextBox Grid.Column="1" Text="{Binding DtcFilter, UpdateSourceTrigger=PropertyChanged}" 
                      Background="#1A1A1A" Foreground="#FFAA00" BorderBrush="#333333" 
                      Padding="12,8" FontSize="14" Margin="0,0,10,0"/>

            <Button Grid.Column="2" Content="SEARCH" Command="{Binding SearchCommand}"
                    Background="#00AAFF" Foreground="White" BorderThickness="0" 
                    Padding="25,8" FontWeight="Bold" Cursor="Hand"/>
        </Grid>

        <ScrollViewer Grid.Row="2" Grid.Column="0" VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding Posts}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Background="#151515" BorderBrush="#222222" BorderThickness="1" 
                                CornerRadius="8" Margin="0,0,0,15" Padding="20">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <TextBlock Grid.Row="0" Text="{Binding Title}" FontSize="18" FontWeight="SemiBold" Foreground="#00AAFF" Margin="0,0,0,10"/>
                                <TextBlock Grid.Row="1" Text="{Binding Content}" Foreground="#CCCCCC" TextWrapping="Wrap" Margin="0,0,0,15" FontSize="13"/>

                                <Grid Grid.Row="2">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="By " Foreground="#666666" FontSize="11"/>
                                        <TextBlock Text="{Binding AuthorName}" Foreground="#AAAAAA" FontSize="11" FontWeight="Bold"/>
                                    </StackPanel>

                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                        <TextBlock Text="{Binding Upvotes}" Foreground="#00FF00" VerticalAlignment="Center" FontWeight="Bold" Margin="0,0,10,0"/>
                                        <Button Content="▲" Command="{Binding DataContext.UpvoteCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}" CommandParameter="{Binding}" Background="Transparent" Foreground="#666666" BorderThickness="0"/>
                                    </StackPanel>
                                </Grid>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <Border Grid.Row="2" Grid.Column="1" Background="#111111" BorderBrush="#222" BorderThickness="1,0,0,0" Margin="20,0,0,0" Padding="15">
            <StackPanel>
                <TextBlock Text="KNOWLEDGE GRAPH" FontSize="14" FontWeight="Bold" Foreground="#00AAFF" Margin="0,0,0,15"/>
                <ItemsControl ItemsSource="{Binding RelatedNodes}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Background="#1A1A1A" BorderBrush="#333" BorderThickness="1" CornerRadius="4" Margin="0,0,0,10" Padding="10">
                                <StackPanel>
                                    <TextBlock Text="{Binding Name}" FontWeight="Bold" Foreground="#FFAA00"/>
                                    <TextBlock Text="{Binding Type}" FontSize="10" Foreground="#666"/>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </Border>

        <Border Grid.Row="2" Grid.ColumnSpan="2" Background="#80000000" Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <ProgressBar IsIndeterminate="True" Width="200" Height="4" Foreground="#00AAFF" Background="#1A1A1A" BorderThickness="0"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
