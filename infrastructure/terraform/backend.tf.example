# Template for S3 Remote State
# Usage: Copy to backend.tf and fill in values or use -backend-config during init
/*
terraform {
  backend "s3" {
    bucket         = "hope-terraform-state"
    key            = "environments/staging/terraform.tfstate"
    region         = "us-east-1"
    encrypt        = true
    dynamodb_table = "terraform-lock"
  }
}
*/
